import React from 'react';
import OAuthLoginButton from './OAuthLoginButton';

const App = () => {
	return (
		<React.Fragment>
			<OAuthLoginButton />
		</React.Fragment>
	);
};

export default App;
